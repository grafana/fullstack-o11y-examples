{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Local overrides layered on top of the org-inherited Renovate config: group non-major updates per ecosystem to stop the per-lockfile PR pileup, and automerge the safe tiers once CI is green. Major updates stay separate for manual review.",
  "prConcurrentLimit": 10,
  "prHourlyLimit": 4,
  "rebaseWhen": "behind-base-branch",
  "platformAutomerge": false,
  "vulnerabilityAlerts": {
    "groupName": "{{#if isMajor}}{{{packageFileDir}}} {{{depNameSanitized}}} security major{{else}}{{{manager}}} security non-major{{/if}}"
  },
  "lockFileMaintenance": {
    "enabled": true,
    "schedule": ["before 6am on monday"]
  },
  "packageRules": [
    {
      "description": "Group Go non-major updates into one PR (all share go/.../services/go.mod+go.sum).",
      "matchManagers": ["gomod"],
      "matchUpdateTypes": ["minor", "patch", "digest", "pin"],
      "groupName": "go non-major"
    },
    {
      "description": "Group Python non-major updates into one PR.",
      "matchManagers": ["pip_requirements", "pip-compile", "pep621", "poetry"],
      "matchUpdateTypes": ["minor", "patch", "digest", "pin"],
      "groupName": "python non-major"
    },
    {
      "description": "Group Java/Maven non-major updates into one PR.",
      "matchManagers": ["maven"],
      "matchUpdateTypes": ["minor", "patch", "digest", "pin"],
      "groupName": "java non-major"
    },
    {
      "description": "Group Ruby/Bundler non-major updates into one PR.",
      "matchManagers": ["bundler"],
      "matchUpdateTypes": ["minor", "patch", "digest", "pin"],
      "groupName": "ruby non-major"
    },
    {
      "description": "Group Node.js/npm non-major updates into one PR.",
      "matchManagers": ["npm"],
      "matchUpdateTypes": ["minor", "patch", "digest", "pin"],
      "groupName": "nodejs non-major"
    },
    {
      "description": "Group container base-image non-major updates into one PR.",
      "matchManagers": ["dockerfile", "docker-compose"],
      "matchUpdateTypes": ["minor", "patch", "digest", "pin"],
      "groupName": "docker images non-major"
    },
    {
      "description": "Automerge the safe tiers (patch/minor/digest/pin) once branch checks pass. Covers security patch/minor updates too. Excludes 0.x deps (matchCurrentVersion !/^0/) because SemVer permits breaking changes in pre-1.0 minor releases; those are handled by the manual rule below. Requires the branch-protection ruleset to allow the Renovate app to bypass required review (see PR description).",
      "matchUpdateTypes": ["minor", "patch", "digest", "pin"],
      "matchCurrentVersion": "!/^0/",
      "automerge": true,
      "automergeType": "pr"
    },
    {
      "description": "Do not automerge pre-1.0 (0.x) non-major updates. SemVer allows breaking changes in 0.x minor releases (Renovate warns about this), and this repo has many 0.x deps (e.g. OpenTelemetry packages). Keep them grouped but require manual review.",
      "matchUpdateTypes": ["minor", "patch", "digest", "pin"],
      "matchCurrentVersion": "/^0/",
      "automerge": false,
      "addLabels": ["needs-manual-review"]
    },
    {
      "description": "Never automerge major updates; keep them separate and flag for manual review (potential breaking changes).",
      "matchUpdateTypes": ["major"],
      "automerge": false,
      "addLabels": ["needs-manual-review"]
    }
  ]
}
